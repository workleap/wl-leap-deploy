id: multi-env-service
workloads:
  api-gateway:
    kind: api
    image:
      repository: api-gateway
    buildConfig:
      path: schemas/v0/tests/multi-env/src/ApiGateway/ApiGateway.csproj
    ingress:
      pathPrefix: /gateway
    replicas: 1
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
    autoscaling:
      horizontal:
        enabled: false
